pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Weather_Platform"
include(":androidWeatherPlatform")
include(":sharedWeatherPlatform")